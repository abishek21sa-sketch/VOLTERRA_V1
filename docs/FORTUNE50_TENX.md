# Fortune-50 TENX analytical layer

## Identity
- Original algorithm: **GRIDWEAVE-v1**
- Unique predictive-learning family: **Graph-regularized spatiotemporal demand learning**
- Analytical AI role: **AI Network Planner**
- Workspaces: **20**

## Required decision contract
The bundled analytical AI must predict a domain outcome, quantify uncertainty, invoke the repository-authored algorithm, challenge a naive alternative, identify the downstream OR/simulation escalation, provide operator actions and abstention conditions, and keep autonomous operational execution disabled.

## Windows test
```powershell
Set-ExecutionPolicy -Scope Process Bypass
.\scripts\windows_tenx_acceptance.ps1
.\scripts\start_tenx_workstation.ps1
```

The validation evidence generated by the first command is written to `artifacts/tenx_validation.json`.

## Evidence boundary
The bundled TENX scenario is deterministic synthetic/reference evidence intended to validate algorithmic integration and product behavior. It is not production, clinical, financial, or field validation. Existing repository-native acceptance gates remain authoritative for the rest of the platform.
